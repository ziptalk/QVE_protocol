import styled from "styled-components";

function Main (){

const TopContainer = styled.div`
background: #1B1A1E;
`;

const EContainer = styled.div`

`;
    return (
        <TopContainer>
        <EContainer style={{height: '500px'}}></EContainer>
        </TopContainer>
    );
}

export default Main;